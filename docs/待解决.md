# 待解决清单

## 当前方向

后续以 `原生文本v3` 为主线继续，不再优先推进以下路线：

- 整张 PNG 顶层覆盖 PSD。
- Python/JSX 重新绘制卡片、标题条、阴影和图标。

原因：

- 顶层覆盖不可编辑，且无法沉淀为可复用模板。
- 重画组件虽然能变整齐，但会丢失原 PSD 的设计质感。
- `原生组件v1` 已验证：只替换原 PSD 组件内容，视觉上更接近原版。
- `原生组件v2` 在 v1 基础上补上透明图表和红字强调，是当前更接近可验收的基础版本。
- `原生文本v3` 在 v2 基础上把红黑正文升级为 Photoshop 可编辑 `TEXT` 图层，是当前正式 PSD 路线的基线。

## P0：原生文本 v3 仍需继续打磨

### 1. 单层富文本已并入正式 v3，排版精度仍需打磨

现象：

- v3 最初通过多层 Photoshop `TEXT` 图层恢复红黑正文，并且 PSD 内可编辑。
- 2026-05-28 已单独验证：首屏第一段可以升级为单个 `PARAGRAPHTEXT` 图层，并通过 `textStyleRange` 实现局部标红。
- 测试 PSD 中新正文图层数量为 1，`textStyleRange` 数量为 9，未产生拆句式 `native_v3/top_intro` 图层。
- 2026-05-28 已并入正式 `native_v3`：7 个正文块均输出为 `native_v3_rich_text_*` 单层文本，旧拆句层数量为 0。
- 当前字号、行距和字重是脚本近似值，和原 PSD 人工排版仍可能存在细节差异。

原因判断：

- v1 的问题来自整段替换 `textItem.contents`，会丢失局部样式。
- v2 先解决视觉结果，暂时用透明图片绕过 Photoshop 局部字符样式问题。
- v3 已从多层文字升级为单层段落富文本，后续重点转为字号、行距、字重和文本框高度的精调。

建议修复：

- 保持单层段落富文本：`PARAGRAPHTEXT + textStyleRange`。
- 建立每个正文区域的字号、行距、字重标准。
- 回读 PSD，检查每个富文本正文区域只有 1 个 `TEXT` 图层，且 `textStyleRange` 覆盖红黑片段。

### 2. 图表和表格细节仍不够像原版

现象：

- `表1`、国债收益率曲线、资金利率曲线已经放进原 PSD 占位层。
- v2 已把曲线图背景改为透明，避免盖住 PSD 原卡片背景。
- v3 仍沿用这一图表/表格方案。
- 但图表字体、线条、坐标轴、图例和原版仍有差异。
- 2026-05-28 已确认：Word 图表 rels 记录了外部 Excel/OLE 来源，两个图表都指向 `D:\MyWorkSpace\新Strats\招行工作\日常工作\金葵花-数据底表.xlsx`。

原因判断：

- 当前图表由脚本绘制，主要保证数据和大致结构。
- 尚未完全复刻原版 Excel/PSD 图表样式。
- 因此当前版本只能说“近似绘制”，不能说已经完全等同手工版。
- 如果拿不到原始 Excel，只能用 DOCX chart XML 数据加脚本样式近似，不能保证完全等同手工版。

建议修复：

- 最优路径：找到 Word 图表外部引用的原始 Excel：`D:\MyWorkSpace\新Strats\招行工作\日常工作\金葵花-数据底表.xlsx`，直接从 Excel/Office 导出图表图片，再放回 PSD 占位层。这样最接近人工版。
- 当前流程已把外部 Excel 链接写入 `psd_content/*.json` 的 `assets.chart_external_workbooks`，后续可据此提示缺失底表或切换到 Excel 导出路线。
- 次优路径：继续读取 DOCX 内 `word/charts/chart*.xml` 的数据，同时解析 `word/charts/style*.xml` 和 `colors*.xml`，把 Office 图表样式转成脚本参数。
- 如果继续纯脚本绘制，需要建立图表视觉规范：
  - 字号。
  - 线宽。
  - 坐标轴范围。
  - 图例位置。
  - 图片边距。
- 表格需要单独建立表格视觉规范：
  - 表头底色和单元格底色。
  - 外框线、内框线宽度。
  - 日期格式和小数位。
  - 红绿数值颜色。
  - 字号、字重、行高。
- 替换到原 PSD 占位层时，保持比例和清晰度。

### 3. 动态空隙已并入正式富文本流程，后续继续精调

现象：

- 2026-05-28 已在原始 PSD 基础上验证：内容放入后，按真实 `bounds.bottom` 缩放白底板和内层卡片，再顺排后续模块。
- 原版 `native_v3` 动态空隙输出高度从固定 6893px 缩到 6502px。
- 单层富文本测试中，首屏白底板从 1958 调整到 1683，下方模块整体上移 275px。
- 单层富文本并入正式流程后，原版输出高度为 6486px。

原因判断：

- 旧流程的问题不是 PSD 组件不能自适应，而是没有在内容替换后根据真实图层边界重排。
- Photoshop 原 PSD 中标题卡、标题文字、图标存在 linked layers，顺排时必须按链接组去重移动，避免标题耳朵重复上移。

建议修复：

- 正式流程继续从原始 PSD 打开，不从改过的 PSD 二次修改。
- 先创建单层富文本、表格和图表，再执行 `layout_adjustments.dynamic_gaps`。
- 对 linked layers 做已处理记录，保证标题卡、标题文字、图标只随所在模块移动一次。
- 后续按人工版继续微调各 section 的 bottom gap，而不是回到固定高度。

### 4. PSD 图层定位仍偏脆弱

现象：

- 脚本目前靠文本前缀或图层名查找目标层。
- 遇到前置换行、同名图层、重复文字时可能定位失败或定位错层。

原因判断：

- 原 PSD 图层名不完全规范，存在同名层和隐藏层。
- 复杂文本层的 `contents` 可能包含换行或特殊空格。

建议修复：

- 建立正式图层映射表，记录：
  - 图层路径。
  - 原始 bounds。
  - 用途。
  - 替换策略。
- 后续脚本按图层路径定位，而不是靠文本模糊匹配。

### 5. 三版 Word 已接入结构化 JSON，但尚未接入 PSD 批量导出

现象：

- 三版 Word 已可输出：
  - `weeks/2026-05-25_2026-05-29/work/psd_content/原版.json`
  - `weeks/2026-05-25_2026-05-29/work/psd_content/固收+.json`
  - `weeks/2026-05-25_2026-05-29/work/psd_content/债市.json`
- `原生组件v1` 目前仍只导出原版 PSD/PNG。
- 固收+、债市两个版本还没有进入原生 PSD 组件替换路线。

原因判断：

- 用户已确认 PSD 只需要原版，但三版 PNG 都需要。
- 现在内容层已经拆好，下一步需要让 Photoshop JSX 按 variant JSON 批量替换和导出。

建议修复：

- 原版 PSD 流程稳定后，复用同一 PSD 组件导出三版 PNG。
- 三版只替换首屏标题/摘要，下面主体和固定策略保持一致。

## P1：项目工程化待补

### 1. 需要明确最终生产流程

待确定：

- 最终以 PSD 原生组件替换为主，还是以 PNG 自动生成为主。
- 图表由 Word/Excel 抽取后重绘，还是直接从 Excel 导出图片。
- 三版首段文案由人工给定，还是脚本根据类型生成初稿。

### 2. 需要建立验收标准

建议至少包含：

- 无明显空白。
- 无文字、图标、图表叠加。
- 原 PSD 组件质感保留。
- 关键红字强调保留。
- 所有日期、收益率、bp 变化与 Word/Excel 来源一致。
- 三版只有首段文案不同，主体内容保持一致。
- 长图宽度固定 1125px，最终高度按内容自适应。

### 3. 大文件管理

当前 PSD 单文件约 60M-63M，仍低于 GitHub 100M 限制，但不适合长期频繁提交。

建议：

- 后续引入 Git LFS 管理 `.psd`。
- 输出图和 PSD 分版本归档，避免每次迭代都重复提交大文件。
