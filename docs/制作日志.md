# 制作日志

## 2026-05-28

### native_v3 正式流程规范化

本轮把此前验证过的“单层富文本”和“动态空隙”并入正式 `native_v3` 生成流程，不再停留在测试 PSD。

已更新的正式原则：

- 每次生成都从原始 PSD 打开：`weeks/2026-05-25_2026-05-29/inputs/psd/债市周报.psd`。
- 不从已经改过的 PSD 继续二次修改。
- 保留原 PSD 的标题条、白底板、浅黄滤镜、图标、阴影和水印。
- 7 个红黑正文块全部使用单个 Photoshop `PARAGRAPHTEXT` 图层，通过 `textStyleRange` 做局部标红。
- 内容放入后，再按 Photoshop 返回的真实 `bounds.bottom` 调整白底板、内层卡片和后续 section 位置。
- 空隙是“内容放完后的目标留白”，不是提前写死的 section 高度。

相关代码和配置：

```text
docs/engineering_spec.md
schemas/psd_layer_map.json
scripts/layout_constants.jsx
scripts/prepare_native_v3_content.py
scripts/build_native_psd_v3.jsx
scripts/inspect_native_v3_text_layers.jsx
tests/test_engineering_contract.py
tests/test_native_v3_workflow_contract.py
tests/test_native_v3_editable_text.py
```

正式输出：

```text
weeks/2026-05-25_2026-05-29/outputs/native_v3/
├── 金葵花债市周观察20260521_原版_原生文本v3.png
└── 金葵花债市周观察20260521_原版_原生文本v3.psd
```

生成口径：

```bash
/Users/a123/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
  scripts/prepare_native_v3_content.py \
  --variant 原版 \
  --display-date 2026-5-27 \
  --source-date 2026年5月22日

osascript -e 'tell application "Adobe Photoshop 2025" to do javascript file "/Users/a123/Downloads/债市周观察/债市周观察/scripts/build_native_psd_v3.jsx"'
```

校验结果：

- 输出图尺寸：`1125 × 6486`。
- 头部日期：`2026-5-27`。
- 数据来源：`数据来源：wind，截至2026年5月22日`。
- PSD 回读：`native_v3_rich_text_*` 单层富文本正文块 `7` 个。
- 旧拆句式 `native_v3/...` 正文层数量：`0`。
- `style_ranges` 数量分别为：`11, 9, 3, 5, 7, 9, 2`。
- 自动测试：`tests.test_engineering_contract`、`tests.test_native_v2_assets`、`tests.test_native_v3_editable_text`、`tests.test_native_v3_workflow_contract`，共 `22` 个测试通过。

### 图表和表格处理口径

本轮确认：当前图表/表格仍是脚本近似绘制，不假装已经完全等同手工版。

已识别到 Word 图表 rels 中记录了外部 Excel/OLE 来源，两个图表都指向：

```text
D:\MyWorkSpace\新Strats\招行工作\日常工作\金葵花-数据底表.xlsx
```

处理原则：

- 最像原版的修法：拿到上述 Excel 底表，用 Excel/Office 直接导出图表图片，再放回 PSD 占位层。
- 拿不到 Excel 时：继续解析 DOCX 内 `word/charts/chart*.xml` 的数据，并补充解析 `style*.xml`、`colors*.xml`，把 Office 样式转成脚本参数。
- 纯脚本绘制只能继续逼近字体、线宽、坐标轴范围、图例位置、表格行高、边框和红绿颜色，不能保证完全等同手工版。

流程更新：

- `scripts/extract_word_psd_content.py` 新增 `external_chart_workbooks()`，把图表外部 Excel 来源写入 `psd_content/*.json` 的 `assets.chart_external_workbooks`。
- `schemas/psd_content_schema.json` 已补充 `chart_external_workbooks` 字段说明。
- [docs/待解决.md](待解决.md) 已把 Excel 导出图表路线列为图表高保真修复的优先方案。

## 2026-05-26

### 周度目录整理

将本轮项目文件归入制作周目录：

```text
weeks/2026-05-25_2026-05-29/
├── inputs/
│   ├── psd/
│   ├── word/
│   │   ├── raw/
│   │   ├── edited/
│   │   └── variants/
│   └── reference_long_images/
├── work/
└── outputs/
```

处理原则：

- 根目录保留 `scripts/`、`schemas/`、`docs/`，作为项目通用能力。
- 每周素材、临时工作产物和输出放到对应 `weeks/<date-range>/`。
- 当前制作周由 `scripts/project_paths.py` 统一指定。

### 三版 Word 到 PSD 输入层

用户补充了三版 Word：

```text
/Users/a123/Downloads/债市周观察/债市周观察/weeks/2026-05-25_2026-05-29/inputs/word/variants/
├── 债市周观察原版.docx
├── 债市周观察（固收+）.docx
└── 债市周观察（债市）.docx
```

对比结果：

- 三版均为 17 个有效段落、1 个表、2 个图表。
- 除顶部投资建议段外，其他段落一致。
- 表格内容一致。
- 两个图表 XML 一致。
- 因此三版 Word 可以作为统一内容母版，只把顶部投资建议作为版本变量。

本轮新增：

```text
schemas/psd_content_schema.json
schemas/red_rules.json
scripts/extract_word_psd_content.py
docs/内容拆解与PSD输入规则.md
```

已实现：

- 三版 Word 转结构化 JSON。
- 配置策略作为固定模块，不再从 Word 中抽取。
- 自动标红规则沉淀到 `schemas/red_rules.json`。
- 债市版顶部冲突口径在 JSON 层修正为：
  - `虽然短期或仍有波动，但仍有较多机构存在欠配压力，债市多头趋势或仍能延续。`
- `scripts/prepare_native_v1_content.py` 已接入 `weeks/2026-05-25_2026-05-29/work/psd_content/原版.json`，不再继续硬编码首屏原版文案。

验证输出：

```text
weeks/2026-05-25_2026-05-29/work/psd_content/
├── 原版.json
├── 固收+.json
├── 债市.json
└── index.json
```

已重新调用 Photoshop 2025 执行 `scripts/build_native_psd_v1.jsx`，生成本周目录下的原版 PSD/PNG：

```text
weeks/2026-05-25_2026-05-29/outputs/native_v1/
├── 金葵花债市周观察20260521_原生组件v1.png
└── 金葵花债市周观察20260521_原生组件v1.psd
```

校验：

- PNG 尺寸：`1125 × 7037`。
- PSD 文件约 `62.8MB`。

下一步：

- Photoshop JSX 需要读取 `rich_text` 红黑片段，而不是整段替换文本。
- PSD 区块高度需要根据文本实际高度自适应，不再通过统一字数解决。

## 2026-05-25

### 素材梳理

- 确认项目文件位于 `/Users/a123/Downloads/债市周观察/债市周观察`。
- 找到原始 PSD：`债市周报.psd`。
- 找到原始 Word：`金葵花债市周度复盘20260521.docx`。
- 找到修改版 Word：`金葵花债市周度复盘20260521 - 修改版本.docx`。
- 找到旧版参考长图：
  - `长图/原版.png`
  - `长图/固收+.png`
  - `长图/债.png`

### 内容口径

- 本轮以修改版 Word 作为主要内容来源。
- 原始 Word 只用于理解结构和对照。
- 用户确认：
  - 三版长图都需要 PNG。
  - PSD 只需要原版。
  - 策略部分暂时不变，沿用原来的配置策略。
  - 三个版本主要区别在开头一段话，下方主体基本一致。

### 技术处理

- 解析 Word 正文、表格和图表数据。
- 从 DOCX chart XML 中提取：
  - 国债收益率曲线。
  - DR001/DR007 资金利率曲线。
- 用 `scripts/build_long_images.py` 生成三版 PNG。
- 用 Photoshop JSX 读取 PSD 图层信息，确认原长图位于 PSD 右侧 `x=3042`、宽度 `1125`。
- 用 `scripts/make_original_psd_overlay.jsx` 将新版原版 PNG 贴入原 PSD，生成新版 PSD。
- 用 `scripts/export_psd_right_preview.jsx` 从新版 PSD 导出右侧预览，并与原版 PNG 像素比对。

### 已生成文件

```text
长图/20260521新版/
├── 金葵花债市周观察20260521_原版.png
├── 金葵花债市周观察20260521_固收+.png
├── 金葵花债市周观察20260521_债.png
└── 金葵花债市周观察20260521_原版.psd
```

### 校验结果

- `金葵花债市周观察20260521_原版.png`：1125 × 7037。
- `金葵花债市周观察20260521_固收+.png`：1125 × 7291。
- `金葵花债市周观察20260521_债.png`：1125 × 7203。
- `金葵花债市周观察20260521_原版.psd`：4167 × 7037。
- Photoshop 导出的 PSD 右侧预览与原版 PNG 像素一致。

### 当前质量结论

当前版本只能作为自动化草稿和流程验证，不能作为最终长图模板。

用户已指出两个核心问题：

- 中间空白很多。
- 部分区域发生叠加。

这些问题已经记录到 [待解决.md](待解决.md)，下一阶段应优先解决版式生成逻辑，而不是继续局部补丁。

### GitHub 同步准备

- 目标仓库：`https://github.com/LLJ-code1/zhaishichangtu`
- 当前本地目录原先不是 git 仓库。
- 已将内层目录 `/Users/a123/Downloads/债市周观察/债市周观察` 初始化为 git 仓库。
- 已将远端设置为 `https://github.com/LLJ-code1/zhaishichangtu.git`。
- 已完成初始提交：`b8b2f1f 整理债市周观察长图项目`。
- 首次推送因大文件 pack 出现 HTTP 400，设置 `http.version=HTTP/1.1` 和 `http.postBuffer=524288000` 后推送成功。
- GitHub CLI 当前提示 token 失效；普通 Git 推送成功，但后续如果要用 `gh` 创建 PR、issue 或 release，需要重新登录 GitHub CLI。

### 基础长图 v0：重画主体组件实验

目标：

- 放弃“整张 PNG 顶层覆盖”的方式。
- 尝试在 PSD 内新建 `基础长图v0_主体` 图层组。
- 正文使用可编辑文本层，表格/图表作为图片层。
- 用自动累加 y 坐标减少空白和叠加。

产出：

```text
长图/基础长图v0/
├── 金葵花债市周观察20260521_基础v0.png
└── 金葵花债市周观察20260521_基础v0.psd
```

阶段结论：

- 结构上比第一版 overlay 更整齐，基本解决了文字/图标叠加。
- 但视觉上明显不像原版，因为主体卡片、标题条、阴影、图标、浅黄滤镜都是脚本重新画的。
- 关键教训：**使用同一个 PSD 文件，不等于沿用了 PSD 的设计语言。必须复用 PSD 原有图层组件。**

### 原生组件 v1：PSD 原有组件替换实验

目标：

- 保留原 PSD 内已有的设计组件。
- 不重画卡片、不重画标题条、不重新造阴影。
- 只替换已有文字层、表格层和图表层。

复用的核心 PSD 元素：

- `版块1标题卡`
- `版块1白底板`
- `版块1浅黄滤镜`
- 首屏卡片结构
- `债市表现 / 债市分析 / 后市展望 / 配置策略` 标题条
- 资金面和市场风险偏好图标
- `表1`
- `图片1`
- `图片2`
- 风险提示文字层

新增脚本：

```text
scripts/prepare_basic_v0_assets.py
scripts/prepare_native_v1_content.py
scripts/build_native_psd_v1.jsx
```

产出：

```text
长图/原生组件v1/
├── 金葵花债市周观察20260521_原生组件v1.png
└── 金葵花债市周观察20260521_原生组件v1.psd
```

校验结果：

- PNG 尺寸：`1125 × 7037`，与原版长图一致。
- PSD 约 60M，PNG 约 23M。
- 视觉质感明显回到原版体系，说明“原生组件替换”方向正确。

仍需继续处理：

- 替换整段文字会丢失原 PSD 中的红字强调样式。
- 表格和图表目前仍是脚本生成图片，细节未完全复刻原版。
- 原生组件高度仍主要沿用原 PSD 固定高度，后续需要在保留组件质感的前提下做自适应。

### 原生组件 v2：透明图表与富文本资产修正

触发问题：

- 国债收益率曲线被脚本导出为带浅色底的整张图片，放回 PSD 后盖住了原卡片背景。
- 首屏摘要和正文红字强调丢失。
- 首屏摘要实际内容未缺失，但因为固定 Photoshop 文本框高度不足，商品和贵金属尾句被裁掉。

处理方式：

- `scripts/prepare_basic_v0_assets.py` 的曲线图背景改为透明 RGBA。
- 新增 `scripts/prepare_native_v2_content.py`：
  - 重新生成透明图表资产。
  - 按 `schemas/red_rules.json` 生成带红黑分段的透明富文本 PNG。
  - 输出 `weeks/2026-05-25_2026-05-29/work/native_v2/content.json`。
- 新增 `scripts/build_native_psd_v2.jsx`：
  - 继续保留 PSD 原有卡片、标题条、图标、滤镜和阴影。
  - 将红字段落作为透明图片贴回原文本层 bounds。
  - 图表只保留线条、坐标轴和图例，露出 PSD 原卡片背景。

产出：

```text
weeks/2026-05-25_2026-05-29/outputs/native_v2/
├── 金葵花债市周观察20260521_原生组件v2.png
└── 金葵花债市周观察20260521_原生组件v2.psd
```

校验结果：

- PNG 尺寸：`1125 × 7037`。
- 图表资产 alpha 通道已验证存在透明像素，左上角 alpha 为 0。
- 首屏摘要已出现红字强调，商品/贵金属尾句完整显示。
- 债市分析区原先的文字叠加问题在 v2 中已通过富文本块替换缓解。

阶段限制：

- v2 的红字段落在 PSD 中是透明图片，不是可编辑 Photoshop 文本层。
- 这是为了先解决视觉验收问题；后续如果要完全可编辑，需要继续研究多文本层或 Photoshop 局部字符样式。

### 原生文本 v3：可编辑 PSD 路线

目标：

- 用户明确要求最终 PSD 可以编辑。
- 在保留原 PSD 卡片、标题条、图标、滤镜和阴影的基础上，把红黑正文从透明图片升级为 Photoshop 原生文字层。

处理方式：

- 新增 `scripts/prepare_native_v3_content.py`：
  - 读取 `psd_content/原版.json` 的 `rich_text`。
  - 使用本机安装的思源黑体 CN：`SourceHanSansCN-Medium`。
  - 在 Python 侧预先计算每一行、每个红黑文本片段的位置。
  - 保留 `1.`、`2.`、`固收+`、`5月LPR`、`DR007` 等英文数字组合和符号。
- 新增 `scripts/build_native_psd_v3.jsx`：
  - 隐藏原 PSD 中对应的整段旧文本层。
  - 在原文本层 bounds 内创建 `native_v3_text_*` 图层组。
  - 每个红黑片段都是独立 Photoshop `TEXT` 图层，可在 PSD 内直接编辑。
- 新增 `scripts/inspect_native_v3_text_layers.jsx`：
  - 回读生成后的 PSD，检查 v3 图层是否真的是文字层、字体和颜色是否正确。

产出：

```text
weeks/2026-05-25_2026-05-29/outputs/native_v3/
├── 金葵花债市周观察20260521_原生文本v3.png
└── 金葵花债市周观察20260521_原生文本v3.psd
```

校验结果：

- PNG 尺寸：`1125 × 7037`。
- PSD 内检测到 `75` 个 v3 正文图层，全部为 Photoshop `TEXT` 图层。
- 字体：`SourceHanSansCN-Medium`。
- 颜色：黑色 `24,22,20`，红色 `232,31,31`。
- 已验证保留：`1.`、`2.`、`固收+`、`5月LPR`、`DR007`。
- 自动测试：`tests.test_native_v3_editable_text` 和 `tests.test_native_v2_assets` 通过。

当时阶段限制：

- v3 的正文可编辑，但为了实现红黑分段，一个段落会拆成多个文字层。此项已在 2026-05-28 升级为单层 `PARAGRAPHTEXT + textStyleRange`。
- 图表和表格仍然是图片层，不是 Photoshop 原生图表。此项仍未完全解决，后续优先走 Excel/Office 导出图表路线。
- 当前整体高度仍沿用原 PSD 固定高度，还不是完整自适应长图。此项已在 2026-05-28 接入动态空隙流程，后续继续精调留白。
